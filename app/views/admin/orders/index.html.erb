<% title "Orders" %>

<div class="col-lg-12 grid-margin stretch-card">
              <div class="card">
                <div class="card-body">
                  <%= link_to "Add Order", new_admin_order_path, class: "btn btn-outline-primary float-right" %>
                   <h4 class="card-title">Orders List</h4>
                  <div class="table-responsive">
                    <table class="table table-striped">
                      <thead>
                        <tr>
                          <th>
                            Order Number
                          </th>
                          <th>
                            Name
                          </th>
                          <th>
                            Status
                          </th>
                          <th>
                            Total Price
                          </th>
                          <th>
                            Created 
                          </th>
                           <th>
                            Link
                          </th>
                        </tr>
                      </thead>
                      <tbody>
                        <% @orders.each do |order| %>
                              <tr>
                                <td>
                                  <%= order.order_number %>
                                </td>
                                <td>
                                  Order Name(TBC)
                                </td>
                                <td>
                                  <%= order.status %>
                                </td>
                                <td>
                                  €<%= order.total_price %>
                                </td>
                                <td>
                                  <%= order.created_at.strftime("%b %e, %Y") %>
                                </td>
                                <td>
                                  <%= link_to admin_order_path(order.id)  do %>
                                  View
                                  <% end %>
                                </td>
                            </tr>
                        <% end %>
                      </tbody>
                    </table>
                  </div>
                </div>
              </div>
            </div>
